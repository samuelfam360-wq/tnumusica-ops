# Copy this file to .env.local for local development.
# On Vercel, set these same three under Project Settings > Environment Variables instead.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only — never exposed to the browser. From console.anthropic.com > API Keys.
ANTHROPIC_API_KEY=
